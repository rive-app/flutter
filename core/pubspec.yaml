name: core
description: A new Flutter package project.
version: 0.0.1

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^1.1.0
  meta: ^1.1.8
  binary_buffer:
    path: ../dependencies/binary-buffer-dart
  fractional:
    path: ../dependencies/fractional-dart

dev_dependencies:
  test: ^1.9.4