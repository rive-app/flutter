name: peon
description: SQS based worker for Rive.
homepage: https://rive.app/

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.8.4 <3.0.0"

dependencies:
  aws_client: 
    git: 
      url: https://github.com/rive-app/aws_client.git
      path: aws_client/
  http_client: ^1.4.0
  args: ^1.5.3
  utilities:
    path: ../utilities
  cupertino_icons: ^0.1.3
  logging: ^0.11.4
  

dev_dependencies:
  pedantic: ^1.9.0
