{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run web",
            "type": "shell",
            "command": "flutter run --release --dart-define=FLUTTER_WEB_USE_SKIA=true -d chrome",
            "problemMatcher": [
                "$dart-pub-build_runner"
            ],
            "isBackground": true
        }
    ]
}